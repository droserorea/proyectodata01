<?php
function drupalcustomenodes_controller_theme($existing, $type, $theme, $path){
  return array(
    'customnodes_create'=>array(
      'variables'=> array('msgCreate'=>'text msg', 'title'=>'text to title')
    )
  );
}
